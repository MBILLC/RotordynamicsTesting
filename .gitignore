**/*.ipynb_checkpoints/

# Generated regression-report output (batch notebooks + raw funnel-comparison
# CSV trace data) -- reproducible from a regression run via ModCheck's
# regression_testing.py, not a stored baseline. Mirrors ModCheck's own
# .gitignore for the same kind of content (HtmlReports/InteractiveReports/
# NotebookReports/). Resources/ReferenceResults/ IS the stored baseline and
# stays tracked.
Resources/Notebooks/
